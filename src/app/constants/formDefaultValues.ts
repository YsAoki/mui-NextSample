/** 入力値が空の場合 */
export const EMPTY_INPUT = "";

/** ラジオボタン、セレクトボックスが空の場合 */
export const EMPTY_VALUE = "";

/** チェックボックスが未選択状態の場合(length値の検証で使用) */
export const EMPTY_CHECKBOX = 0;

/** DatePicker の初期値（未選択の場合） */
export const EMPTY_DATE = null;