/**SelectBoxで使用する型 */
export type MenuItemType = {
  text: string;
  value: string | number;
};

/** ラジオボタン、チェックボックスで使用する型 */
export type RadioAndCheckType = {
  label: string;
  value: string | number;
};
